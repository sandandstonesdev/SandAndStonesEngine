#version 450

layout(set = 0, binding = 0) uniform texture2D SurfaceTexture;
layout(set = 0, binding = 1) uniform sampler SurfaceSampler;

layout(location = 0) in vec4 fsin_Color;
layout(location = 1) in vec2 fsin_texCoords;

layout(location = 0) out vec4 fsout_Color;

void main()
{
    //vec4 texColor = texture(sampler2D(SurfaceTexture, SurfaceSampler), fsin_texCoords);
    //fsout_Color =  vec4(texColor.xyz * fsin_Color.xyz, texColor.w * fsin_Color.w);
    fsout_Color = fsin_Color;
}